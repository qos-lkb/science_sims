---
description: HKDSE 科學模擬平台核心脈絡與文件索引
alwaysApply: true
---

# Science Simulations — 核心脈絡

本儲存庫是 **HKDSE／中學科學** 用的互動模擬集合：多為 **獨立 HTML 頁**，可 **靜態託管**（GitHub Pages），或以 **PHP + `index.csv`** 動態產生入口（LAMPP）。

## 技術取向

- **HTML5 + Vanilla JS**；樣式以 **Tailwind CSS（CDN）** 為主；部分模擬用 Three.js、Chart.js、MathJax、少數用 React + Babel standalone。
- **勿**在未要求時引入打包工具、新框架或重寫整站架構。
- 變更應與同目錄、同類型檔案的既有風格一致。

## 目錄約定

- `physics/`（含 `01/`…`e03/` 等單元）、`science/`、`biology/`、`chem/`、`astronomy/`、`other/` 等 — 各放對應模擬 HTML。
- 詳細檔名規則、無障礙與品質準則見專案根目錄 **`rule.md`**；架構與依賴見 **`architecture.md`**；使用者說明見 **`README.md`**。

## 助理行為

- 僅修改與任務直接相關的檔案；新增模擬時遵守 `rule.md` 的命名與 HTML 結構。
- 使用者偏好以 **繁體中文** 說明與討論（程式註解可依既有檔案語言）。
